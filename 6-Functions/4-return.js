// Example of a function using the return keyword

function multiply(a, b) {
    return a * b;
}

// Storing the returned value in a variable
let result = multiply(5, 4);
console.log("The result of multiplication is: " + result);

// Using the returned value directly in a statement
console.log("The square of 10 is: " + multiply(10, 10));