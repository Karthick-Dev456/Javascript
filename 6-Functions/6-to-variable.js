// Example of allocating a function to a variable (Function Expression)

const greet = function(name) {
    return "Hello, " + name + "!";
};

// Calling the function using the variable name
console.log(greet("John"));

// Function expressions are useful for passing functions as arguments 
// or defining them conditionally.